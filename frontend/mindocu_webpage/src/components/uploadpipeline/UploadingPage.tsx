import { Box } from '@mui/material';



export default function UploadingPage() {
    return (
        <div>
            <Box sx={{ display: 'flex', flexDirection: 'column', height: '100%' }}>
                <h1>Fall wird erstellt...</h1>
                <p>Verarbeitung wird abgeschlossen</p>
            </Box>
        </div>
    );
}